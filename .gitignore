# Xcode
build/
DerivedData/
*.xcuserstate
*.xcscmblueprint
xcuserdata/
*.moved-aside

# NOTE: project.yml is the source of truth; regenerate with `xcodegen generate`.
# The .xcodeproj is committed (matching the pourcraft convention) so clone-and-open works.

# Swift Package Manager
.swiftpm/
.build/
Package.resolved

# macOS
.DS_Store

# App data / secrets
*.xcconfig.local
GoogleService-Info.plist

# Obsidian (wiki vault) — keep notes, ignore per-machine workspace/cache state
docs/wiki/.obsidian/workspace*.json
docs/wiki/.obsidian/cache
docs/wiki/.trash/
